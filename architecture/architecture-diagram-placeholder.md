# Architecture Diagram Placeholder

TODO(public-review): Add a public architecture diagram.

Draft structure:

```text
Ideas and literature
       |
       v
Candidate signal families
       |
       v
Validation checks: OOS, cost, lag, turnover
       |
       v
Sleeve library
       |
       v
Portfolio construction: caps and redundancy review
       |
       v
Implementation checks: costs, lag, turnover
       |
       v
Reproducibility layer: artifact lineage, data vintages, hash manifests
       |
       v
Delayed aggregate notes
```

Do not include private source file names, broker services, current holdings, account details, or proprietary formulas in the final diagram.
